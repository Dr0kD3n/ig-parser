---
description: Automated linting and formatting across the project
---

// turbo-all

1. Run linting for backend

```powershell
cd backend; npm run lint -- --fix
```

2. Run linting for frontend

```powershell
cd frontend; npm run lint -- --fix
```

3. Verify all builds

```powershell
./build.bat
```
